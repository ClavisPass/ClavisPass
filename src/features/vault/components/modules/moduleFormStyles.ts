import { StyleSheet } from "react-native";

const moduleFormStyles = StyleSheet.create({
  inputShell: {
    height: 40,
    flex: 1,
    flexShrink: 1,
    minWidth: 0,
  },
  input: {
    minWidth: 0,
    width: "100%",
  },
  row: {
    gap: 8,
    minWidth: 0,
  },
});

export default moduleFormStyles;
